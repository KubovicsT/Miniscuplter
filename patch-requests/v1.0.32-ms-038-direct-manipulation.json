{
  "schema": 1,
  "target_branch": "v1.0.32",
  "expected_head_sha": "c3805837997d5d9111d4e53a5aca0a6113d284a4",
  "target_path": "Scripts/Main.V1018Foundation.cs",
  "expected_blob_sha": "7c46242a6527d67ff324a999283b0a051fd2bb54",
  "commit_message": "feat: add direct viewport manipulation",
  "patch": "--- a/Scripts/Main.V1018Foundation.cs\n+++ b/Scripts/Main.V1018Foundation.cs\n@@ -17,6 +17,10 @@\n     bool _v1018ViewportInstalled;\n     bool _v1018Dragging;\n+    bool _v1018DirectMoveDragging;\n+    Vector3 _v1018DirectMovePlaneNormal;\n+    Vector3 _v1018DirectMoveStartWorld;\n+    Vector3 _v1018DirectMoveObjectStart;\n     Vector3 _v1018HandleAxis = Vector3.Zero;\n     Vector3 _v1018DragStartPosition;\n     Vector3 _v1018DragStartRotation;\n@@ -40,6 +44,7 @@\n         if (!_v1019ViewportPipelineInstalled)\n             V1017SyncViewport();\n         V1017UpdateGizmo();\n+        V1018EnsureRotationRings();\n         if (_v1018ToolStatus != null)\n         {\n             string selected = _selected == null ? \"none\" : _selected.Name.ToString();\n@@ -76,7 +81,7 @@\n         tools.AddChild(_v1018ToolStatus);\n         tools.AddChild(new Label\n         {\n-            Text = \"RMB orbit · MMB pan · wheel zoom\\nMove/Rotate/Scale: drag a colored gizmo axis\",\n+            Text = \"RMB orbit · MMB pan · wheel zoom\\nMove: drag the model freely or an axis precisely\\nRotate: drag a colored ring; Scale: drag an axis\",\n             AutowrapMode = TextServer.AutowrapMode.WordSmart\n         });\n         host.AddChild(_v1018ToolOverlay);\n@@ -110,6 +115,7 @@\n         _panning = false;\n         _sculpting = false;\n         _v1018Dragging = false;\n+        _v1018DirectMoveDragging = false;\n         SetStatus($\"Viewport tool: {_v1018Tool}\");\n     }\n@@ -157,12 +163,31 @@\n                 {\n                     if (_v1018Tool is V1018ViewportTool.Move or V1018ViewportTool.Rotate or V1018ViewportTool.Scale)\n                     {\n+                        if (_v1018Tool == V1018ViewportTool.Rotate &&\n+                            V1018TryHitRotationRing(button.Position, out var ringAxis))\n+                        {\n+                            BeginV1018Transform(ringAxis);\n+                            return;\n+                        }\n+\n                         if (V1018TryHitGizmo(button.Position, out var axis))\n                         {\n                             BeginV1018Transform(axis);\n                             return;\n                         }\n \n+                        if (_v1018Tool == V1018ViewportTool.Move &&\n+                            V1018RaycastScene(button.Position, out var directTarget))\n+                        {\n+                            V1027SelectStableViewportHit(directTarget);\n+                            RebuildSceneList();\n+                            V1017UpdateGizmo();\n+                            if (BeginV1018DirectMove(button.Position))\n+                                return;\n+                        }\n+\n                         V1018SelectAt(button.Position);\n                         return;\n                     }\n@@ -181,6 +206,7 @@\n                     _sculpting = false;\n                     _v1018Dragging = false;\n+                    _v1018DirectMoveDragging = false;\n                     _v1018HandleAxis = Vector3.Zero;\n                 }\n@@ -210,7 +236,10 @@\n \n         if (_v1018Dragging)\n         {\n-            ApplyV1018Transform(delta);\n+            if (_v1018DirectMoveDragging)\n+                ApplyV1018DirectMove(motion.Position);\n+            else\n+                ApplyV1018Transform(delta);\n             return;\n         }\n@@ -268,6 +297,78 @@\n         return axis != Vector3.Zero;\n     }\n+\n+    void V1018EnsureRotationRings()\n+    {\n+        if (_v1017Gizmo == null || !IsInstanceValid(_v1017Gizmo)) return;\n+\n+        void Ring(string name, Vector3 rotationDegrees, Color color)\n+        {\n+            MeshInstance3D? ring = _v1017Gizmo.GetChildren().OfType<MeshInstance3D>()\n+                .FirstOrDefault(child => child.Name.ToString().Equals(name, StringComparison.Ordinal));\n+            if (ring == null)\n+            {\n+                ring = new MeshInstance3D\n+                {\n+                    Name = name,\n+                    Mesh = new TorusMesh\n+                    {\n+                        InnerRadius = .78f,\n+                        OuterRadius = .86f,\n+                        Rings = 48,\n+                        RingSegments = 8\n+                    },\n+                    RotationDegrees = rotationDegrees,\n+                    MaterialOverride = new StandardMaterial3D\n+                    {\n+                        AlbedoColor = color,\n+                        ShadingMode = BaseMaterial3D.ShadingModeEnum.Unshaded,\n+                        CullMode = BaseMaterial3D.CullModeEnum.Disabled\n+                    }\n+                };\n+                _v1017Gizmo.AddChild(ring);\n+            }\n+            ring.Visible = _v1018Tool == V1018ViewportTool.Rotate;\n+        }\n+\n+        Ring(\"X rotation ring\", new Vector3(0, 0, 90), new Color(.95f, .22f, .18f));\n+        Ring(\"Y rotation ring\", Vector3.Zero, new Color(.24f, .90f, .35f));\n+        Ring(\"Z rotation ring\", new Vector3(90, 0, 0), new Color(.20f, .48f, 1f));\n+    }\n+\n+    bool V1018TryHitRotationRing(Vector2 screenPosition, out Vector3 axis)\n+    {\n+        axis = Vector3.Zero;\n+        if (_camera == null || _v1017Gizmo == null || !IsInstanceValid(_v1017Gizmo) || !_v1017Gizmo.Visible)\n+            return false;\n+\n+        Vector3 origin = _camera.ProjectRayOrigin(screenPosition);\n+        Vector3 direction = _camera.ProjectRayNormal(screenPosition);\n+        float best = float.PositiveInfinity;\n+        foreach (var ring in _v1017Gizmo.GetChildren().OfType<MeshInstance3D>())\n+        {\n+            string name = ring.Name.ToString();\n+            if (!ring.Visible || !name.Contains(\"rotation ring\", StringComparison.OrdinalIgnoreCase)) continue;\n+            if (!V1018RayMeshSafe(origin, direction, ring, out Vector3 hit)) continue;\n+            float distance = origin.DistanceSquaredTo(hit);\n+            if (distance >= best) continue;\n+            best = distance;\n+            axis = name.StartsWith(\"X\", StringComparison.OrdinalIgnoreCase)\n+                ? Vector3.Right\n+                : name.StartsWith(\"Y\", StringComparison.OrdinalIgnoreCase)\n+                    ? Vector3.Up\n+                    : Vector3.Back;\n+        }\n+\n+        return axis != Vector3.Zero;\n+    }\n \n     static bool V1018RayMeshSafe(Vector3 origin, Vector3 direction, MeshInstance3D obj, out Vector3 hit)\n@@ -316,6 +417,60 @@\n         return found;\n     }\n+\n+    bool BeginV1018DirectMove(Vector2 screenPosition)\n+    {\n+        if (_selected == null || !IsInstanceValid(_selected) || _camera == null) return false;\n+        Vector3 normal = -_camera.GlobalTransform.Basis.Z.Normalized();\n+        if (!V1018TryRayPlanePoint(screenPosition, _selected.GlobalPosition, normal, out Vector3 start))\n+            return false;\n+\n+        _v1018Dragging = true;\n+        _v1018DirectMoveDragging = true;\n+        _v1018HandleAxis = Vector3.Zero;\n+        _v1018DragAmount = 0;\n+        _v1018DragStartPosition = _selected.Position;\n+        _v1018DragStartRotation = _selected.Rotation;\n+        _v1018DragStartScale = _selected.Scale;\n+        _v1018DirectMovePlaneNormal = normal;\n+        _v1018DirectMoveStartWorld = start;\n+        _v1018DirectMoveObjectStart = _selected.GlobalPosition;\n+        SetStatus($\"Move {_selected.Name} freely in the view plane; use colored axes for constrained movement.\");\n+        return true;\n+    }\n+\n+    void ApplyV1018DirectMove(Vector2 screenPosition)\n+    {\n+        if (!_v1018DirectMoveDragging || _selected == null || !IsInstanceValid(_selected)) return;\n+        if (!V1018TryRayPlanePoint(screenPosition, _v1018DirectMoveObjectStart, _v1018DirectMovePlaneNormal, out Vector3 current))\n+            return;\n+\n+        Vector3 worldDelta = current - _v1018DirectMoveStartWorld;\n+        Vector3 localDelta = worldDelta;\n+        if (_selected.GetParent() is Node3D parent)\n+            localDelta = parent.GlobalTransform.Basis.Inverse() * worldDelta;\n+        _selected.Position = _v1018DragStartPosition + localDelta;\n+        V1017UpdateGizmo();\n+    }\n+\n+    bool V1018TryRayPlanePoint(Vector2 screenPosition, Vector3 point, Vector3 normal, out Vector3 hit)\n+    {\n+        hit = default;\n+        if (_camera == null) return false;\n+        Vector3 origin = _camera.ProjectRayOrigin(screenPosition);\n+        Vector3 direction = _camera.ProjectRayNormal(screenPosition);\n+        float denominator = normal.Dot(direction);\n+        if (Math.Abs(denominator) < 1e-5f) return false;\n+        float distance = normal.Dot(point - origin) / denominator;\n+        if (!float.IsFinite(distance) || distance < 0) return false;\n+        hit = origin + direction * distance;\n+        return true;\n+    }\n \n     void BeginV1018Transform(Vector3 axis)\n     {\n         if (_selected == null || !IsInstanceValid(_selected)) return;\n         _v1018Dragging = true;\n+        _v1018DirectMoveDragging = false;\n         _v1018HandleAxis = axis;\n"
}
