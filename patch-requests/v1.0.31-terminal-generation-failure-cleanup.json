{
  "schema": 1,
  "target_branch": "v1.0.31",
  "expected_head_sha": "e3ef6383ba0a4e31e8ceedfbe771ff2cb46447f6",
  "target_path": "Scripts/Main.V1020StageCBridge.cs",
  "expected_blob_sha": "fa54a981ca9a1552e0d36fe2d71ec56f725f2041",
  "commit_message": "fix: retire failed generation envelope durably",
  "patch": "diff --git a/Scripts/Main.V1020StageCBridge.cs b/Scripts/Main.V1020StageCBridge.cs\n--- a/Scripts/Main.V1020StageCBridge.cs\n+++ b/Scripts/Main.V1020StageCBridge.cs\n@@ -219,6 +219,8 @@\n         {\n             double sec = (DateTime.UtcNow - _v1093DStarted).TotalSeconds;\n             string detail = V108FriendlyAiError(ex);\n+            string? cleanupWarning = await V1020RetireFailedGenerationAsync(_v1020GenerationBinding);\n+            if (cleanupWarning != null) detail += \" Durable generation cleanup also failed: \" + cleanupWarning;\n             SetV1093DResult($\"3D status: FAILED after {sec:0}s.\", detail);\n             SetStatus(\"3D AI error: \" + detail);\n             V109ShowError(\"2D → 3D generation failed\", detail);\n@@ -240,6 +242,29 @@\n         }\n     }\n \n+    async Task<string?> V1020RetireFailedGenerationAsync(GenerationJobBinding? binding)\n+    {\n+        if (binding == null) return null;\n+        try\n+        {\n+            await _v1020StageCGate.WaitAsync();\n+            try\n+            {\n+                var session = _v1020StageCSession\n+                    ?? throw new InvalidOperationException(\"Stage-C project session was unavailable while retiring a failed generation job.\");\n+                if (StageCGeneration.AbandonGenerationJob(session, binding))\n+                    await V1020SaveSessionAsync();\n+            }\n+            finally { _v1020StageCGate.Release(); }\n+            return null;\n+        }\n+        catch (Exception cleanupEx)\n+        {\n+            return cleanupEx.Message;\n+        }\n+    }\n+\n     async void V1020CancelStageCGeneration()\n     {\n         if (!_v1093DBusy) return;\n"
}
