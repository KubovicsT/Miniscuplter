name: diag-token
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: echo diagnostic
