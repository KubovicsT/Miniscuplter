name: diag-release
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create v0.0.0-diagnostic --notes diagnostic
