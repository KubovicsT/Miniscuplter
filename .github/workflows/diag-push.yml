name: diag-push
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          git tag diagnostic-test HEAD
          git push origin refs/tags/diagnostic-test
